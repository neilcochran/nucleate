# Supply-chain hardening: never run dependency lifecycle scripts on install.
# A malicious or compromised (transitive) dependency cannot execute a
# postinstall hook in this repo. Applies to local and CI installs; workflows
# mirror it with `npm ci --ignore-scripts`. The publishable build is produced
# explicitly via `npm run build`, not an install/publish lifecycle hook.
ignore-scripts=true
